# Backend Runtime Data

This folder is for generated backend artifacts:

- `ltm/`: per-session short-term and long-term memory JSON.
- `baseline_embedding.json`: mean persona embedding used by anomaly detection.
- `knowledge_graph.json`: optional GraphRAG concept graph override.

The app works without these files during scaffolding.
